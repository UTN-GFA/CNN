# The URL the site will be built for
base_url = "https://utn-gfa.github.io/"

theme = "zola-clean-blog"
taxonomies = [
    {name = "categories", rss = true, paginate_by=5},
    {name = "tags", rss = true, paginate_by=5},
]

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

[markdown]
# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

[extra]
author = "Evelyn Kesler"

clean_blog_social = [
    {url = "https://github.com/UTN-GFA", icon="fab fa-github fa-lg"}
]

clean_blog_menu = [
    {url = "$BASE_URL", name = "Inicio"},
    {url = "$BASE_URL/about", name = "Conocenos"},
    {url = "$BASE_URL/contact", name = "Contacto"}
]